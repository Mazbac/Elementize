openapi: 3.1.0
info:
  title: Elementize Content Actions
  version: 0.37.1
  description: Natural-language targeting plus guarded content editing, media handoff, and Pixfort icon lookup for existing Elementor + Pixfort pages.
servers:
  - url: https://example.invalid
    description: Replaced automatically by the Elementize setup screen.

paths:
  /wp-json/elementize/v1/status:
    get:
      operationId: getElementizeStatus
      summary: Check Elementize, Elementor, and Pixfort readiness
      responses:
        '200': { description: Elementize status and supported content-editing scope }

  /wp-json/elementize/v1/pages:
    get:
      operationId: listElementizePages
      summary: List editable Elementor pages
      parameters:
        - { name: q, in: query, required: false, schema: { type: string } }
        - { name: page, in: query, required: false, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: per_page, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 100, default: 30 } }
      responses:
        '200': { description: Editable Elementor pages }

  /wp-json/elementize/v1/pages/{id}/structure:
    get:
      operationId: getElementizePageStructure
      summary: Read the semantic Elementor page map
      description: Returns sections, containers, cards/groups, widgets, snippets, parent/sibling relationships, and optional exact editable fields from the main Elementor document.
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, minimum: 1 } }
        - { name: q, in: query, required: false, schema: { type: string } }
        - { name: element_id, in: query, required: false, schema: { type: string } }
        - { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 300, default: 120 } }
        - { name: include_fields, in: query, required: false, schema: { type: boolean, default: false } }
      responses:
        '200': { description: Semantic page structure and repeated sibling groups }

  /wp-json/elementize/v1/pages/{id}/resolve:
    post:
      operationId: resolveElementizeTargets
      summary: Resolve natural language or screenshot clues to exact Elementor targets
      description: Use natural descriptions, visual clues extracted from a screenshot, and prior selection IDs. Can expand one matched item to siblings, its repeated card group, or its whole section.
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, minimum: 1 } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [description]
              properties:
                description: { type: string, minLength: 1, maxLength: 1000 }
                visual_clues:
                  type: array
                  maxItems: 12
                  items: { type: string, maxLength: 300 }
                context_element_ids:
                  type: array
                  maxItems: 30
                  items: { type: string, maxLength: 100 }
                scope_element_id: { type: string, maxLength: 100 }
                expand: { type: string, enum: [auto, none, siblings, group, section], default: auto }
                expected_count: { type: integer, minimum: 1, maximum: 50 }
      responses:
        '200': { description: Ranked candidates, reusable selection context, and exact fresh editable fields }
        '400': { description: Invalid resolver request }
        '409': { description: Requested scope no longer exists }

  /wp-json/elementize/v1/pages/{id}/content:
    get:
      operationId: getElementizePageContent
      summary: Read editable copy, links, images, and Pixfort icons from one Elementor page
      description: Returns exact element IDs, setting paths, current values, page identity, and a fresh content hash. Safe human-readable strings are discovered beyond a small fixed field-name list.
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, minimum: 1 } }
        - { name: kind, in: query, required: false, schema: { type: string, enum: [all, text, link, media, pixfort_icon], default: all } }
        - { name: element_id, in: query, required: false, schema: { type: string } }
        - { name: top_level_id, in: query, required: false, schema: { type: string } }
        - { name: q, in: query, required: false, schema: { type: string } }
        - { name: offset, in: query, required: false, schema: { type: integer, minimum: 0, default: 0 } }
        - { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 500, default: 100 } }
      responses:
        '200': { description: Fresh editable page-content snapshot }
    post:
      operationId: updateElementizePageContent
      summary: Guardedly update copy, links, images, or Pixfort icons
      description: Use exact fresh fields from resolveElementizeTargets or getElementizePageContent. Requires page status/title/hash and expected values. Live published pages require explicit confirmation.
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, minimum: 1 } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [expected_status, expected_title, content_hash, updates, confirm_content_write]
              properties:
                expected_status: { type: string }
                expected_title: { type: string }
                content_hash: { type: string }
                confirm_content_write: { type: boolean, enum: [true] }
                confirm_published_page: { type: boolean, default: false }
                updates:
                  type: array
                  minItems: 1
                  maxItems: 50
                  items:
                    type: object
                    additionalProperties: false
                    required: [kind, element_id, setting_path]
                    properties:
                      kind: { type: string, enum: [text, link, media, pixfort_icon] }
                      element_id: { type: string, maxLength: 100 }
                      setting_path:
                        type: array
                        minItems: 1
                        maxItems: 30
                        items:
                          anyOf:
                            - { type: string, maxLength: 200 }
                            - { type: integer }
                      expected_value: { type: string }
                      value: { type: string }
                      expected_attachment_id: { type: integer, minimum: 0 }
                      attachment_id: { type: integer, minimum: 1 }
      responses:
        '200': { description: Content changes saved and verified in persisted Elementor data }
        '400': { description: Unsafe content field/value or missing confirmation }
        '409': { description: Page, revision capability, or target changed after the fresh read }

  /wp-json/elementize/v1/media/images:
    get:
      operationId: searchElementizeMediaImages
      summary: Search WordPress Media Library images
      parameters:
        - { name: q, in: query, required: false, schema: { type: string } }
        - { name: filename, in: query, required: false, schema: { type: string } }
        - { name: page, in: query, required: false, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: per_page, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 50, default: 20 } }
      responses:
        '200': { description: Matching WordPress image attachments }

  /wp-json/elementize/v1/media/images/import-chat-file:
    post:
      operationId: importElementizeConversationImage
      summary: Import one image attached in this ChatGPT conversation
      description: Use when the user wants an image they supplied in the current ChatGPT conversation placed on the WordPress page. Returns a WordPress attachment_id for a later guarded media update.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [openaiFileIdRefs, confirm_import]
              properties:
                openaiFileIdRefs:
                  type: array
                  minItems: 1
                  maxItems: 1
                  description: Exactly one user-supplied image from the current ChatGPT conversation. At runtime ChatGPT expands the file reference into file metadata for Elementize.
                  items: { type: string }
                title: { type: string }
                alt: { type: string }
                confirm_import: { type: boolean, enum: [true] }
      responses:
        '201': { description: Conversation image imported and verified }
        '400': { description: Invalid file, unsupported image, or confirmation missing }

  /wp-json/elementize/v1/media/images/import-generated:
    post:
      operationId: importElementizeGeneratedImage
      summary: Import the image ChatGPT just generated into WordPress
      description: 'Use after ChatGPT generates an image the user wants on the page. Pass exactly one source: openaiFileIdRefs when available, otherwise an allowed OpenAI-hosted generated_image_url.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [confirm_import]
              properties:
                openaiFileIdRefs:
                  type: array
                  minItems: 1
                  maxItems: 1
                  description: Exactly one image generated in the current ChatGPT conversation, when ChatGPT can pass it as a file reference.
                  items: { type: string }
                generated_image_url:
                  type: string
                  format: uri
                  maxLength: 8192
                  description: Fallback direct HTTPS URL for the generated image. Elementize accepts this only from an allowed OpenAI file host.
                title: { type: string }
                alt: { type: string }
                confirm_import: { type: boolean, enum: [true] }
      responses:
        '201': { description: Generated ChatGPT image imported and verified }
        '400': { description: Missing or unsafe generated-image source, unsupported image, or confirmation missing }
        '502': { description: Generated image download failed }

  /wp-json/elementize/v1/media/images/import-url:
    post:
      operationId: importElementizeRemoteImage
      summary: Import one public HTTPS image into the WordPress Media Library
      description: Use after the user supplies an image URL or ChatGPT finds and verifies a suitable image online. Returns a WordPress attachment_id for a later guarded media update.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [url, confirm_import]
              properties:
                url: { type: string, format: uri, maxLength: 8192 }
                source_page_url: { type: string, format: uri, maxLength: 8192, description: Optional page where the image was found for provenance. }
                title: { type: string }
                alt: { type: string }
                confirm_import: { type: boolean, enum: [true] }
      responses:
        '201': { description: Remote image imported and verified }
        '400': { description: Unsafe URL, unsupported image, or confirmation missing }
        '502': { description: Remote image download failed }

  /wp-json/elementize/v1/pixfort/icons:
    get:
      operationId: searchElementizePixfortIcons
      summary: Search the Pixfort icon library discovered from the installed Pixfort/theme assets
      description: Returns exact Pixfort icon values suitable for a later guarded pixfort_icon content update. Do not invent icon identifiers; use an exact returned value.
      parameters:
        - { name: q, in: query, required: false, schema: { type: string } }
        - { name: style, in: query, required: false, schema: { type: string, enum: [all, line, duotone, solid], default: all } }
        - { name: page, in: query, required: false, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: per_page, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 100, default: 30 } }
      responses:
        '200': { description: Matching installed Pixfort icon values }
        '503': { description: Installed Pixfort icon library could not be indexed }
