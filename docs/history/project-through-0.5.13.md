# Historical project tracker through 0.5.13

The repository previously kept a long V0.1–0.5.13 project/status log in the root `PROJECT.md`. That snapshot became misleading once Design Intelligence progressed through the 0.2x line.

The full original tracker remains recoverable in Git history immediately before the repository cleanup (`cleanup/repository-structure` parent history, including commit `7e2bc7dfd7ce66dabf4f584b76d983ace4b3986e`).

This history marker exists so old V0.1–0.5.13 terminology is not confused with current runtime/project status. Durable integration discoveries from that era remain in `DISCOVERY.md`.
